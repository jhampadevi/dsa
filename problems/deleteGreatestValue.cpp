#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int deleteGreatestValue(vector<vector<int>> &grid)
{

        sort[grid.begin(), grid.end(), 1];
}

int grid[3][3] = {{1, 2, 4}, {3, 3, 1}};